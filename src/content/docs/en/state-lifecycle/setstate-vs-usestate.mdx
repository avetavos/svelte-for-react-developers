---
title: 'setState vs useState'
description: 'StatefulWidget + State + setState(() => ...) compared to React useState — why Flutter splits the widget from its state class.'
---

import ReactFlutter from '../../../../components/ReactFlutter.tsx';
import DartPad from '../../../../components/DartPad.tsx';
import Diff from '../../../../components/Diff.astro';
import Quiz from '../../../../components/Quiz.tsx';
import ProgressTracker from '../../../../components/ProgressTracker.tsx';

In React, `useState` is a hook that stores a value and returns a setter. Call the setter, React schedules a re-render, the component function runs again with the new value. The state lives in a hidden slot attached to the component instance.

Flutter's equivalent is `setState(() { ... })`. But before you can call it, you need two classes instead of one — a `StatefulWidget` and a `State<T>`. That split looks verbose at first, but it solves a specific problem: widget objects in Flutter are immutable and may be recreated cheaply by the framework. The `State` object is longer-lived and holds the actual mutable data.

## Side-by-side comparison

export const ssReact = `import { useState } from 'react';

function Counter() {
  const [count, setCount] = useState(0);

  return (
    <div>
      <p>Count: {count}</p>
      <button onClick={() => setCount(count + 1)}>
        Increment
      </button>
    </div>
  );
}`;

export const ssFlutter = `class Counter extends StatefulWidget {
  const Counter({super.key});

  @override
  State<Counter> createState() => _CounterState();
}

class _CounterState extends State<Counter> {
  int _count = 0;           // state lives here

  void _increment() {
    setState(() {           // wraps the mutation
      _count++;
    });
  }

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Text('Count: \$_count'),
        ElevatedButton(
          onPressed: _increment,
          child: const Text('Increment'),
        ),
      ],
    );
  }
}`;

<ReactFlutter client:visible react={ssReact} flutter={ssFlutter} />

<Diff title="useState → StatefulWidget + setState">

- **Two classes, not one.** `StatefulWidget` holds immutable configuration (analogous to props). `State<T>` holds mutable fields and owns `build()`. Flutter creates one `State` object per widget instance and keeps it alive across rebuilds.
- **`setState(() { ... })` takes a callback** that performs the mutation directly on the field (`_count++`). React's setter replaces the value; Flutter's callback mutates in place. Flutter reads the new value from the field, not from a returned snapshot.
- **`createState()`** is the factory that creates the `State` object. It is called once when the widget is first inserted into the tree — equivalent to the first render in React.
- **Fields, not destructured tuples.** Whereas React returns `[value, setter]`, Flutter state fields are plain Dart instance variables (`int _count = 0`). The leading underscore is a Dart convention for private members.
- **Rebuild scope.** In React every `useState` setter triggers a full re-render of the component function. In Flutter `setState` triggers a rebuild of the `build()` method on the same `State` object — the scope is identical in practice for single widgets.

</Diff>

## Runnable example

export const ssCode = `import 'package:flutter/material.dart';

void main() => runApp(const MyApp());

class MyApp extends StatelessWidget {
  const MyApp({super.key});
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'Counter',
      theme: ThemeData(colorScheme: ColorScheme.fromSeed(seedColor: Colors.indigo)),
      home: const Scaffold(
        body: Center(child: Counter()),
      ),
    );
  }
}

class Counter extends StatefulWidget {
  const Counter({super.key});

  @override
  State<Counter> createState() => _CounterState();
}

class _CounterState extends State<Counter> {
  int _count = 0;

  void _increment() => setState(() => _count++);
  void _decrement() => setState(() => _count--);
  void _reset()     => setState(() => _count = 0);

  @override
  Widget build(BuildContext context) {
    final scheme = Theme.of(context).colorScheme;
    return Column(
      mainAxisAlignment: MainAxisAlignment.center,
      children: [
        Text(
          '\$_count',
          style: TextStyle(
            fontSize: 72,
            fontWeight: FontWeight.bold,
            color: scheme.primary,
          ),
        ),
        const SizedBox(height: 24),
        Row(
          mainAxisAlignment: MainAxisAlignment.center,
          children: [
            FilledButton.tonal(onPressed: _decrement, child: const Text('−')),
            const SizedBox(width: 12),
            FilledButton(onPressed: _increment, child: const Text('+')),
            const SizedBox(width: 12),
            OutlinedButton(onPressed: _reset, child: const Text('Reset')),
          ],
        ),
      ],
    );
  }
}`;

<DartPad client:visible code={ssCode} />

export const quizSs = [
  {
    q: 'What does Flutter\'s setState() callback do that React\'s useState setter does not?',
    options: [
      'It returns the new state value',
      'It mutates fields in-place rather than replacing the state value',
      'It batches multiple state updates into one',
      'It prevents the widget from rebuilding',
    ],
    answer: 1,
  },
  {
    q: 'How many times is createState() called for a single Counter widget instance?',
    options: [
      'Once per rebuild',
      'Once per setState() call',
      'Once when the widget is first inserted into the tree',
      'Twice — once for mount and once for unmount',
    ],
    answer: 2,
  },
  {
    q: 'Where do mutable state fields live in a Flutter StatefulWidget?',
    options: [
      'On the StatefulWidget class itself',
      'In a separate external store',
      'On the State<T> subclass',
      'In the BuildContext',
    ],
    answer: 2,
  },
  {
    q: 'What is the React equivalent of Flutter\'s State<T>.build()?',
    options: [
      'The useEffect hook body',
      'The component function body / render() method',
      'The useState initializer function',
      'The event handler callback',
    ],
    answer: 1,
  },
];

<Quiz client:visible id="state-lifecycle/setstate-vs-usestate" questions={quizSs} />

<ProgressTracker client:visible id="state-lifecycle/setstate-vs-usestate" />
